PySide6>=6.6,<7.0
PyMuPDF>=1.24,<2.0
pytest>=7.4
pyinstaller>=6.0

# pikepdf no se usa en el MVP (ver README, seccion "Decisiones tecnicas").
# Se deja documentado para cuando se implemente reparacion/validacion
# estructural avanzada de PDF en una fase posterior:
# pikepdf>=8.0,<10.0
